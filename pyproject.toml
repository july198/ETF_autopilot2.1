[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "etf-auto-trader"
version = "0.1.0"
description = "Automation of ETF trading rules from spreadsheet"
requires-python = ">=3.11"
